apiVersion: v1
kind: PersistentVolume
metadata:
  name: grafana-pv-volum
  labels:
    type: local
spec:
  storageClassName: manu
  capacity:
    storage: 3Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/grafana"